#!/bin/sh
set -e
case "$1" in
    remove)
        /bin/sh /usr/share/syswall/prerm.sh || true
        ;;
    purge)
        rm -rf /var/lib/syswall /var/log/syswall
        groupdel syswall 2>/dev/null || true
        ;;
esac
systemctl daemon-reload 2>/dev/null || true
exit 0
